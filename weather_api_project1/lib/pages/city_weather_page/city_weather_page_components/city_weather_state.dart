import 'package:flutter/material.dart';

class CityWeatherState extends StatelessWidget {
  const CityWeatherState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
